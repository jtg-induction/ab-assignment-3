import React from 'react'
import { AppBar, Login } from '@Containers/index'

export const LoginPage = () => {
  return (
    <React.Fragment>
      <AppBar />
      <Login />
    </React.Fragment>
  )
}
