import { SxProps, Theme } from '@mui/system'

const styles: { content: SxProps<Theme> } = {
  content: {
    margin: '0 auto',
    boxShadow: 'none',
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'space-between',
    padding: 1,
  },
}
export default styles
