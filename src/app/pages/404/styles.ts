import { SxProps, Theme } from '@mui/system'
const styles: { errorImage: SxProps<Theme>; root: SxProps<Theme> } = {
  root: {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
  },
  errorImage: {},
}
export default styles
